# abort script on any command that exit with a non zero value
set -e

cp simple_service_broker/simple-service-broker-1.0.0.jar ${BOSH_INSTALL_TARGET}
cp simple_service_broker/manifest.yml ${BOSH_INSTALL_TARGET}
